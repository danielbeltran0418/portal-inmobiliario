# SP4 — Leads y contacto · Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Que un comprador con cuenta pueda contactar sobre una propiedad publicada, y que el vendedor reciba ese lead en una bandeja donde el contacto solo aparece al aceptarlo.

**Architecture:** Dos tablas (`leads` y `leads_contacto`) donde la segunda solo es legible cuando el lead padre salió de `nuevo`, de modo que el revelado lo hace cumplir la base y no la interfaz. La escritura va por una única función `crear_lead()`, porque el lead nace en dos tablas a la vez y `authenticated` no tiene `INSERT` en ninguna. En paralelo, el limitador de intentos de SP0 se generaliza para cubrir `/registro`, que al exigir cuenta pasa a ser la puerta del spam.

**Tech Stack:** Next.js 16.3.3 App Router · React 19.2.8 · TypeScript estricto · Supabase (Postgres 17, Auth, RLS) · Zod 4 · Vitest · Playwright

Spec: `docs/superpowers/specs/2026-09-11-portal-inmobiliario-sp4-design.md`

## Global Constraints

- **Nunca editar una migración ya aplicada.** Toda corrección va en una migración nueva.
- **Numeración de migraciones:** la última aplicada es `20260909000100`. Las de SP4 van `20260911000100` en adelante, en el orden de las tareas.
- **Todo `REVOKE EXECUTE` nombra `PUBLIC` explícitamente.** Postgres concede `EXECUTE` a `PUBLIC` por defecto en toda función nueva; un `REVOKE` que solo mencione `anon` y `authenticated` no quita nada. Ese fue el crítico de SP0.
- **Toda función nueva lleva `SET search_path = ''`** y referencias con esquema explícito (`public.x`, `auth.uid()`).
- **Toda prueba de denegación asserta el código de error Y arregla el caso positivo en la misma prueba.** Sin caso positivo, la prueba pasaría aunque la tabla no existiera.
- **Todo control de seguridad se falsifica:** romperlo a propósito, ver la prueba roja, restaurar, ver la verde. Se pegan las dos salidas en el reporte de la tarea.
- **Idioma:** identificadores, comentarios y mensajes en español, sin tildes en los identificadores de SQL.
- **No se usa `rtk`.** En Windows, `vitest`/`playwright`/`supabase`/`tsc`/`lint` se ejecutan con la herramienta PowerShell; por Bash devuelven JSON que oculta errores reales.
- **Base a superar:** 317 unitarias, 130 RLS, 14 E2E. Cada tarea deja las suites en verde o explica por qué no.
- **`npm run lint` debe quedar en 0 problemas** y `npx tsc --noEmit` sin salida.

## Estructura de ficheros

**Migraciones** (`supabase/migrations/`)

| Fichero | Responsabilidad |
|---|---|
| `20260911000100_intentos_accion.sql` | Renombrar `intentos_login` → `intentos_accion`, añadir `accion`, reescribir las dos funciones. Sin regla nueva |
| `20260911000200_limite_registro.sql` | La rama `'registro'` del predicado |
| `20260911000300_leads.sql` | Enum, las dos tablas, índices, RLS y grants |
| `20260911000400_crear_lead.sql` | La función de escritura y su auditoría |
| `20260911000500_transicion_lead.sql` | Trigger de transición de estado y su auditoría |

**Producción** (`src/`)

| Fichero | Responsabilidad |
|---|---|
| `src/lib/auth/limite-intentos.ts` | *Modificar.* Única frontera con las funciones del limitador |
| `src/app/(auth)/registro/acciones.ts` | *Modificar.* Aplicar el límite y el fallo cerrado sin IP |
| `src/lib/validacion/esquemas.ts` | *Modificar.* `esquemaLead` |
| `src/lib/errores/mapear.ts` | *Modificar.* Mensajes de lead |
| `src/lib/leads/consultas.ts` | *Crear.* Lectura de la bandeja y del contador |
| `src/lib/navegacion/volver.ts` | *Crear.* Validación de la ruta de retorno |
| `src/app/[barrio]/[slug]/formulario-lead.tsx` | *Crear.* El formulario, o el enlace a login |
| `src/app/[barrio]/[slug]/acciones.ts` | *Crear.* Server action que llama a `crear_lead` |
| `src/app/[barrio]/[slug]/page.tsx` | *Modificar.* Montar el formulario |
| `src/app/(vendedor)/panel/leads/page.tsx` | *Crear.* La bandeja |
| `src/app/(vendedor)/panel/leads/acciones.ts` | *Crear.* Aceptar y descartar |
| `src/app/(vendedor)/panel/page.tsx` | *Modificar.* Contador de leads nuevos |

---

### Task 1: Renombrar el limitador, sin cambiar comportamiento

Esta tarea **no añade ninguna regla**. Su único objetivo es que las cinco suites que hoy conocen `intentos_login` queden verdes bajo el nombre nuevo. Si se mezcla con la Task 2 y algo se pone rojo, no se sabrá cuál de los dos cambios fue.

**Files:**
- Create: `supabase/migrations/20260911000100_intentos_accion.sql`
- Modify: `src/lib/auth/limite-intentos.ts`
- Modify: `tests/rls/limite-intentos.test.ts` (40 referencias)
- Modify: `tests/rls/auditoria.test.ts` (6)
- Modify: `tests/unit/limite-intentos.test.ts` (2)
- Modify: `tests/rls/privilegios-anon.test.ts` (1)
- Modify: `tests/unit/accion-registro.test.ts` (1)
- Modify: `src/app/(auth)/registro/acciones.ts` (solo un comentario obsoleto, líneas 33-36)

**Interfaces:**
- Consumes: nada.
- Produces: tabla `public.intentos_accion(id bigserial, accion text, clave text, ip inet NULL, exitoso boolean, creado_en timestamptz)`; funciones `public.accion_bloqueada(p_accion text, p_clave text, p_ip inet) → boolean` y `public.registrar_intento_accion(p_accion text, p_clave text, p_ip inet, p_exitoso boolean) → void`; y en TypeScript `accionBloqueada(accion: string, clave: string, ip: string | null): Promise<boolean>` y `registrarIntentoAccion(accion: string, clave: string, ip: string | null, exitoso: boolean): Promise<boolean>`.

- [ ] **Step 1: Escribir la migración del rename**

Crear `supabase/migrations/20260911000100_intentos_accion.sql`:

```sql
-- ============================================================================
-- Generalizacion del limitador de intentos.
--
-- SP4 exige cuenta para enviar un lead, asi que /registro pasa a ser la puerta
-- del spam. El limitador de SP0 solo cubre el login y esta atado a el hasta en
-- los nombres.
--
-- Esta migracion SOLO renombra y amplia la forma. La regla de registro entra
-- en 20260911000200: separadas a proposito, para que si algo se rompe se sepa
-- cual de los dos cambios fue.
-- ============================================================================

ALTER TABLE public.intentos_login RENAME TO intentos_accion;
ALTER TABLE public.intentos_accion RENAME COLUMN correo TO clave;

-- DEFAULT 'login' para que las filas existentes queden clasificadas, y se
-- retira acto seguido: a partir de aqui quien inserta dice que accion es.
ALTER TABLE public.intentos_accion ADD COLUMN accion text NOT NULL DEFAULT 'login';
ALTER TABLE public.intentos_accion ALTER COLUMN accion DROP DEFAULT;

COMMENT ON COLUMN public.intentos_accion.clave IS
  'Lo que identifica al sujeto del intento. En login es el correo en minusculas.';
COMMENT ON COLUMN public.intentos_accion.accion IS
  'Que se intento: login, registro. Cada una tiene su propio predicado en accion_bloqueada.';

ALTER INDEX IF EXISTS intentos_login_correo_idx RENAME TO intentos_accion_clave_idx;

CREATE INDEX IF NOT EXISTS intentos_accion_ventana_idx
  ON public.intentos_accion (accion, ip, creado_en DESC);

-- ----------------------------------------------------------------------------
-- Las funciones nuevas. El predicado de 'login' es IDENTICO al de
-- 20260831000500: cinco fallidos en quince minutos, y con p_ip nula se cuenta
-- por clave sin discriminar IP, que es un limite MAS estricto.
-- ----------------------------------------------------------------------------
CREATE OR REPLACE FUNCTION public.accion_bloqueada(p_accion text, p_clave text, p_ip inet)
RETURNS boolean LANGUAGE sql STABLE SECURITY DEFINER SET search_path = '' AS $$
  SELECT CASE p_accion
    WHEN 'login' THEN (
      SELECT count(*) >= 5
      FROM public.intentos_accion
      WHERE accion = 'login'
        AND clave = lower(p_clave)
        AND (p_ip IS NULL OR ip = p_ip)
        AND exitoso = false
        AND creado_en > now() - interval '15 minutes'
    )
    -- Una accion desconocida se bloquea. Si alguien anade una accion y olvida
    -- su rama, el fallo es ruidoso y del lado seguro.
    ELSE true
  END;
$$;

CREATE OR REPLACE FUNCTION public.registrar_intento_accion(
  p_accion text, p_clave text, p_ip inet, p_exitoso boolean
) RETURNS void LANGUAGE plpgsql SECURITY DEFINER SET search_path = '' AS $$
BEGIN
  INSERT INTO public.intentos_accion (accion, clave, ip, exitoso)
  VALUES (p_accion, lower(p_clave), p_ip, p_exitoso);

  IF p_exitoso AND p_accion = 'login' THEN
    DELETE FROM public.intentos_accion
    WHERE accion = 'login'
      AND clave = lower(p_clave)
      AND (p_ip IS NULL OR ip = p_ip)
      AND exitoso = false;
  END IF;

  DELETE FROM public.intentos_accion WHERE creado_en < now() - interval '24 hours';
END $$;

DROP FUNCTION IF EXISTS public.login_bloqueado(text, inet);
DROP FUNCTION IF EXISTS public.registrar_intento_login(text, inet, boolean);

-- PUBLIC va nombrado: Postgres le concede EXECUTE por defecto a toda funcion
-- nueva, y estas dos lo son (las viejas se acaban de borrar, no se reemplazan).
REVOKE EXECUTE ON FUNCTION public.accion_bloqueada(text, text, inet)
  FROM PUBLIC, anon, authenticated;
GRANT EXECUTE ON FUNCTION public.accion_bloqueada(text, text, inet)
  TO service_role;

REVOKE EXECUTE ON FUNCTION public.registrar_intento_accion(text, text, inet, boolean)
  FROM PUBLIC, anon, authenticated;
GRANT EXECUTE ON FUNCTION public.registrar_intento_accion(text, text, inet, boolean)
  TO service_role;
```

- [ ] **Step 2: Aplicar y comprobar que las suites se ponen rojas por el nombre**

Run (PowerShell): `npx supabase db reset`

Run: `npm run test:rls`

Expected: FAIL. `tests/rls/limite-intentos.test.ts` y `tests/rls/auditoria.test.ts` fallan porque llaman a `login_bloqueado` / `registrar_intento_login`, que ya no existen. Eso confirma que esas pruebas tocaban de verdad lo que se acaba de renombrar.

- [ ] **Step 3: Actualizar la frontera de TypeScript**

En `src/lib/auth/limite-intentos.ts`, renombrar las dos exportaciones y añadir el parámetro `accion`. Se conserva íntegro el comportamiento de fallo cerrado y los comentarios que lo explican:

```ts
export async function accionBloqueada(
  accion: string, clave: string, ip: string | null,
): Promise<boolean> {
  const { data, error } = await crearClienteAdmin()
    .rpc('accion_bloqueada', { p_accion: accion, p_clave: clave, p_ip: ip })

  // Ante un fallo de infraestructura se falla cerrado: bloquear es mas seguro
  // que dejar pasar intentos ilimitados.
  if (error) {
    registrarFallo('accion_bloqueada', error)
    return true
  }
  return data === true
}

export async function registrarIntentoAccion(
  accion: string, clave: string, ip: string | null, exitoso: boolean,
): Promise<boolean> {
  const { error } = await crearClienteAdmin()
    .rpc('registrar_intento_accion', {
      p_accion: accion, p_clave: clave, p_ip: ip, p_exitoso: exitoso,
    })

  if (error) {
    registrarFallo('registrar_intento_accion', error)
    return false
  }
  return true
}
```

- [ ] **Step 4: Actualizar el server action del login**

En `src/app/(auth)/login/acciones.ts`, cambiar el import y las tres llamadas. **No cambia ninguna otra línea**, y en particular se conserva el bloque que explica por qué un `registrarIntento` fallido se trata como bloqueo:

```ts
import { accionBloqueada, registrarIntentoAccion } from '@/lib/auth/limite-intentos'
// ...
if (await accionBloqueada('login', correo, ip)) {
  return { error: MENSAJE_BLOQUEADO }
}
// ...
const quedoRegistrado = await registrarIntentoAccion('login', correo, ip, false)
// ...
await registrarIntentoAccion('login', correo, ip, true)
```

- [ ] **Step 5: Actualizar los cinco ficheros de prueba**

Sustituir en los cinco: `intentos_login` → `intentos_accion`, `correo` → `clave` (solo cuando se refiera a esa columna), `login_bloqueado` → `accion_bloqueada`, `registrar_intento_login` → `registrar_intento_accion`. Toda llamada gana `'login'` como primer argumento, y toda fila insertada a mano gana `accion: 'login'`.

En `tests/rls/privilegios-anon.test.ts` y en las pruebas de privilegios, las firmas del `REVOKE` cambian: `accion_bloqueada(text, text, inet)` y `registrar_intento_accion(text, text, inet, boolean)`.

En `src/app/(auth)/registro/acciones.ts`, líneas 33-36, el comentario dice «el de intentos_login solo cubre el login». Actualizarlo a que el límite de registro llega en la migración siguiente.

- [ ] **Step 6: Verificar que todo vuelve a verde, sin regla nueva**

Run (PowerShell): `npm run test:rls`
Expected: PASS, 130/130 — **el mismo número que antes**. Si sube o baja, algo se perdió o se duplicó.

Run: `npm run test:unit`
Expected: PASS, 317/317.

Run: `npx tsc --noEmit` → sin salida. `npm run lint` → 0 problemas.

- [ ] **Step 7: Falsificar que el límite de login sigue vivo**

Editar a mano el predicado de `'login'` en la migración cambiando `>= 5` por `>= 500`, `npx supabase db reset`, correr `npm run test:rls`.
Expected: FAIL en la prueba del bloqueo de login.

Restaurar el `>= 5`, `npx supabase db reset`, correr de nuevo.
Expected: PASS.

Pegar las dos salidas en el reporte. Sin esto, no hay prueba de que el rename conservara la regla.

- [ ] **Step 8: Commit**

```bash
git add supabase/migrations/20260911000100_intentos_accion.sql src/lib/auth/limite-intentos.ts "src/app/(auth)/login/acciones.ts" "src/app/(auth)/registro/acciones.ts" tests/
git commit -m "refactor(limite): generalizar intentos_login a intentos_accion

Sin cambio de comportamiento: el predicado de 'login' es identico. Solo
prepara la forma para que 20260911000200 pueda anadir la regla de registro.

Falsificado que la regla sigue viva: con >= 500 la prueba del bloqueo se pone
roja; restaurada a >= 5, verde."
```

---

### Task 2: La regla de límite en el registro

**Files:**
- Create: `supabase/migrations/20260911000200_limite_registro.sql`
- Modify: `src/app/(auth)/registro/acciones.ts`
- Modify: `src/lib/errores/mapear.ts`
- Test: `tests/rls/limite-intentos.test.ts` (añadir), `tests/unit/accion-registro.test.ts` (añadir)

**Interfaces:**
- Consumes: `accion_bloqueada(p_accion, p_clave, p_ip)` y `registrar_intento_accion(...)` de la Task 1; `ipDeConfianza(cabeceras: Headers): string | null` de `src/lib/http/ip-cliente.ts`.
- Produces: la rama `'registro'` del predicado; `MENSAJE_REGISTRO_BLOQUEADO` y `MENSAJE_SIN_IP_CONFIABLE` en `src/lib/errores/mapear.ts`.

- [ ] **Step 1: Escribir la prueba RLS que falla**

Añadir a `tests/rls/limite-intentos.test.ts`:

```ts
it('el limite de registro cuenta EXITOS por ip, no fallos', async () => {
  const admin = clienteAdmin()
  const ip = '203.0.113.77'
  await admin.from('intentos_accion').delete().eq('ip', ip)

  // Tres altas exitosas desde la misma IP: la tercera todavia pasa.
  for (let i = 0; i < 3; i++) {
    const { error } = await admin.rpc('registrar_intento_accion', {
      p_accion: 'registro', p_clave: `alta-${i}@prueba.test`, p_ip: ip, p_exitoso: true,
    })
    expect(error).toBeNull()
  }

  const { data: bloqueado, error } = await admin.rpc('accion_bloqueada', {
    p_accion: 'registro', p_clave: 'alta-4@prueba.test', p_ip: ip,
  })
  expect(error).toBeNull()
  expect(bloqueado).toBe(true)

  // Caso positivo: otra IP no esta bloqueada. Sin esto, la asercion de arriba
  // pasaria aunque la funcion devolviera true para todo.
  const { data: otra } = await admin.rpc('accion_bloqueada', {
    p_accion: 'registro', p_clave: 'alta-4@prueba.test', p_ip: '203.0.113.78',
  })
  expect(otra).toBe(false)

  await admin.from('intentos_accion').delete().eq('ip', ip)
})

it('los registros FALLIDOS no cuentan para el limite', async () => {
  const admin = clienteAdmin()
  const ip = '203.0.113.79'
  await admin.from('intentos_accion').delete().eq('ip', ip)

  // Diez fallos no bloquean: la regla de registro mira exitos. Un limitador
  // que contara fallos aqui no pararia el alta masiva, que es toda exitosa.
  for (let i = 0; i < 10; i++) {
    await admin.rpc('registrar_intento_accion', {
      p_accion: 'registro', p_clave: `fallo-${i}@prueba.test`, p_ip: ip, p_exitoso: false,
    })
  }

  const { data: bloqueado } = await admin.rpc('accion_bloqueada', {
    p_accion: 'registro', p_clave: 'otro@prueba.test', p_ip: ip,
  })
  expect(bloqueado).toBe(false)

  await admin.from('intentos_accion').delete().eq('ip', ip)
})

it('sin ip de confianza, el registro se bloquea en la base', async () => {
  // Para el login, p_ip nula degrada a contar por correo, que es MAS estricto.
  // Para el registro esa degradacion no existe -- cada alta usa otro correo --
  // asi que degradaria a SIN LIMITE. La base falla cerrado.
  const { data: bloqueado, error } = await clienteAdmin().rpc('accion_bloqueada', {
    p_accion: 'registro', p_clave: 'cualquiera@prueba.test', p_ip: null,
  })
  expect(error).toBeNull()
  expect(bloqueado).toBe(true)
})
```

- [ ] **Step 2: Correr y ver que falla**

Run (PowerShell): `npx vitest run tests/rls/limite-intentos.test.ts`
Expected: FAIL. La rama `'registro'` no existe, así que `accion_bloqueada` cae en el `ELSE true` y la segunda prueba (la de los fallos que no cuentan) se pone roja esperando `false`.

- [ ] **Step 3: Escribir la migración**

Crear `supabase/migrations/20260911000200_limite_registro.sql`:

```sql
-- ============================================================================
-- La regla de registro. NO es la del login, y esa es la razon de ser de esta
-- migracion.
--
-- El login cuenta FALLOS: cinco intentos fallidos sobre un correo. El spam de
-- altas son registros EXITOSOS, cada uno con un correo distinto -- un
-- limitador que contara fallos de registro no bloquearia a nadie.
--
-- Y con p_ip nula no hay degradacion posible: "contar por clave" contaria uno,
-- porque cada alta usa otro correo. Para el login esa degradacion es MAS
-- estricta; aqui seria SIN LIMITE, que es justo el modo de fallo que
-- src/lib/http/ip-cliente.ts se escribio para evitar. Se falla cerrado.
-- ============================================================================

CREATE OR REPLACE FUNCTION public.accion_bloqueada(p_accion text, p_clave text, p_ip inet)
RETURNS boolean LANGUAGE sql STABLE SECURITY DEFINER SET search_path = '' AS $$
  SELECT CASE p_accion
    WHEN 'login' THEN (
      SELECT count(*) >= 5
      FROM public.intentos_accion
      WHERE accion = 'login'
        AND clave = lower(p_clave)
        AND (p_ip IS NULL OR ip = p_ip)
        AND exitoso = false
        AND creado_en > now() - interval '15 minutes'
    )
    WHEN 'registro' THEN (
      CASE WHEN p_ip IS NULL THEN true
      ELSE (
        SELECT count(*) >= 3
        FROM public.intentos_accion
        WHERE accion = 'registro'
          AND ip = p_ip
          AND exitoso = true
          AND creado_en > now() - interval '60 minutes'
      )
      END
    )
    ELSE true
  END;
$$;

-- CREATE OR REPLACE conserva la ACL, asi que los REVOKE de 20260911000100
-- siguen en pie. Se repiten porque es barato y deja el fichero autocontenido.
REVOKE EXECUTE ON FUNCTION public.accion_bloqueada(text, text, inet)
  FROM PUBLIC, anon, authenticated;
GRANT EXECUTE ON FUNCTION public.accion_bloqueada(text, text, inet)
  TO service_role;
```

- [ ] **Step 4: Correr y ver que pasa**

Run (PowerShell): `npx supabase db reset` y luego `npx vitest run tests/rls/limite-intentos.test.ts`
Expected: PASS.

- [ ] **Step 5: Añadir los dos mensajes**

En `src/lib/errores/mapear.ts`:

```ts
export const MENSAJE_REGISTRO_BLOQUEADO =
  'Se han creado demasiadas cuentas desde esta conexion. Intenta de nuevo en una hora.'

/**
 * Sin IP de confianza el registro se rechaza, y el mensaje dice que es de
 * configuracion y no del usuario: quien lo lea no puede hacer nada por su
 * cuenta, y quien opere el despliegue tiene que enterarse. La alternativa era
 * dejar pasar el alta sin limite, que es el agujero silencioso que
 * src/lib/http/ip-cliente.ts existe para evitar.
 */
export const MENSAJE_SIN_IP_CONFIABLE =
  'El registro no esta disponible en este momento por un problema de configuracion del servidor.'
```

- [ ] **Step 6: Escribir la prueba unitaria del server action**

Añadir a `tests/unit/accion-registro.test.ts`:

```ts
it('rechaza el registro cuando no hay ip de confianza', async () => {
  vi.mocked(ipDeConfianza).mockReturnValue(null)
  const resultado = await registrarse({}, formDataValida())
  expect(resultado.error).toBe(MENSAJE_SIN_IP_CONFIABLE)
  // No se llega a crear la cuenta.
  expect(signUpEspia).not.toHaveBeenCalled()
})

it('rechaza el registro cuando la ip esta bloqueada', async () => {
  vi.mocked(ipDeConfianza).mockReturnValue('203.0.113.10')
  vi.mocked(accionBloqueada).mockResolvedValue(true)
  const resultado = await registrarse({}, formDataValida())
  expect(resultado.error).toBe(MENSAJE_REGISTRO_BLOQUEADO)
  expect(signUpEspia).not.toHaveBeenCalled()
})

it('registra el intento exitoso para que cuente en la ventana', async () => {
  vi.mocked(ipDeConfianza).mockReturnValue('203.0.113.11')
  vi.mocked(accionBloqueada).mockResolvedValue(false)
  await registrarse({}, formDataValida())
  expect(registrarIntentoAccion).toHaveBeenCalledWith(
    'registro', expect.any(String), '203.0.113.11', true,
  )
})
```

- [ ] **Step 7: Conectar el límite al server action**

En `src/app/(auth)/registro/acciones.ts`, justo después de la validación del formulario y **antes** del captcha:

```ts
const ip = ipDeConfianza(await headers())

/**
 * Sin IP de confianza no hay limite de registro posible, y se rechaza.
 * Consecuencia buscada: un despliegue sin IP_CABECERA_CONFIABLE no permite
 * registrar a nadie. Es ruidoso y se arregla en un minuto; la alternativa es
 * un agujero silencioso.
 */
if (ip === null) {
  return { error: MENSAJE_SIN_IP_CONFIABLE }
}

/**
 * El limite va ANTES del captcha por el mismo motivo que en el login: a una
 * conexion ya bloqueada se le responde sin gastar una peticion a Cloudflare.
 */
if (await accionBloqueada('registro', correoNormalizado, ip)) {
  return { error: MENSAJE_REGISTRO_BLOQUEADO }
}
```

Y tras un `signUp` sin error, antes del redirect:

```ts
// Solo el alta EXITOSA cuenta: es lo que mide esta regla.
await registrarIntentoAccion('registro', correoNormalizado, ip, true)
```

- [ ] **Step 8: Verificar**

Run (PowerShell): `npm run test:unit` → PASS, 320/320 (317 + 3).
Run: `npm run test:rls` → PASS, 133/133 (130 + 3).
Run: `npx tsc --noEmit` → sin salida. `npm run lint` → 0 problemas.

- [ ] **Step 9: Falsificar**

Cambiar en la migración `WHEN p_ip IS NULL THEN true` por `THEN false`, `npx supabase db reset`, correr `npx vitest run tests/rls/limite-intentos.test.ts`.
Expected: FAIL en «sin ip de confianza, el registro se bloquea en la base».

Restaurar, resetear, correr.
Expected: PASS. Pegar las dos salidas.

- [ ] **Step 10: Commit**

```bash
git add supabase/migrations/20260911000200_limite_registro.sql src/lib/errores/mapear.ts "src/app/(auth)/registro/acciones.ts" tests/
git commit -m "feat(limite): limitar el registro por IP, fallando cerrado sin IP fiable

La regla no es la del login: cuenta EXITOS por IP (>= 3 en 60 min), porque el
spam de altas es todo exitoso y con un correo distinto cada vez.

Y sin IP de confianza se rechaza el alta. Para el login, ip nula degrada a
contar por correo, que es mas estricto; aqui degradaria a SIN LIMITE. Un
despliegue sin IP_CABECERA_CONFIABLE no permite registrar, a proposito."
```

---

### Task 3: Modelo de datos de leads

**Files:**
- Create: `supabase/migrations/20260911000300_leads.sql`
- Test: `tests/rls/leads.test.ts`

**Interfaces:**
- Consumes: `public.perfiles`, `public.propiedades`, `public.es_super_admin()`.
- Produces: enum `public.estado_lead` (`'nuevo'|'aceptado'|'descartado'`); tabla `public.leads(id uuid, propiedad_id uuid, comprador_id uuid, vendedor_id uuid, nombre_mostrado text, mensaje text, estado estado_lead, creado_en timestamptz, respondido_en timestamptz NULL)`; tabla `public.leads_contacto(lead_id uuid PK, correo text, telefono text)`.

- [ ] **Step 1: Escribir la prueba de denegación, que falla porque no hay tablas**

Crear `tests/rls/leads.test.ts`:

```ts
import { randomUUID } from 'node:crypto'
import { describe, it, expect } from 'vitest'
import { clienteAdmin, clienteAnonimo, clienteComo, crearUsuarioDePrueba } from './ayudantes'

describe('privilegios de leads y leads_contacto', () => {
  it('anon no lee ni escribe ninguna de las dos tablas', async () => {
    const anon = clienteAnonimo()

    const leerLeads = await anon.from('leads').select('id')
    expect(leerLeads.data ?? []).toEqual([])

    const insertar = await anon.from('leads').insert({
      propiedad_id: randomUUID(), comprador_id: randomUUID(),
      vendedor_id: randomUUID(), nombre_mostrado: 'X', mensaje: 'x'.repeat(20),
    })
    expect(insertar.error?.code).toBe('42501')

    const insertarContacto = await anon.from('leads_contacto').insert({
      lead_id: randomUUID(), correo: 'x@y.test', telefono: '3001234567',
    })
    expect(insertarContacto.error?.code).toBe('42501')

    // Caso positivo: service_role si puede leer las dos tablas. Sin esto, las
    // aserciones de arriba pasarian aunque las tablas no existieran.
    const admin = clienteAdmin()
    expect((await admin.from('leads').select('id').limit(1)).error).toBeNull()
    expect((await admin.from('leads_contacto').select('lead_id').limit(1)).error).toBeNull()
  })

  it('authenticated no puede insertar ni actualizar leads_contacto', async () => {
    const correo = `lead-priv-${randomUUID()}@prueba.test`
    await crearUsuarioDePrueba({ correo, password: 'LeadPriv2026*', rol: 'comprador' })
    const cliente = await clienteComo(correo, 'LeadPriv2026*')

    const insertar = await cliente.from('leads_contacto').insert({
      lead_id: randomUUID(), correo: 'x@y.test', telefono: '3001234567',
    })
    expect(insertar.error?.code).toBe('42501')

    const actualizar = await cliente.from('leads_contacto')
      .update({ telefono: '3009999999' }).eq('lead_id', randomUUID())
    expect(actualizar.error?.code).toBe('42501')

    const borrar = await cliente.from('leads_contacto').delete().eq('lead_id', randomUUID())
    expect(borrar.error?.code).toBe('42501')
  })

  it('authenticated no puede insertar en leads directamente', async () => {
    const correo = `lead-ins-${randomUUID()}@prueba.test`
    await crearUsuarioDePrueba({ correo, password: 'LeadIns2026*', rol: 'comprador' })
    const cliente = await clienteComo(correo, 'LeadIns2026*')

    const insertar = await cliente.from('leads').insert({
      propiedad_id: randomUUID(), comprador_id: randomUUID(),
      vendedor_id: randomUUID(), nombre_mostrado: 'X', mensaje: 'x'.repeat(20),
    })
    expect(insertar.error?.code).toBe('42501')
  })
})
```

- [ ] **Step 2: Correr y ver que falla**

Run (PowerShell): `npx vitest run tests/rls/leads.test.ts`
Expected: FAIL con `relation "public.leads" does not exist` (código `42P01`), no `42501`.

- [ ] **Step 3: Escribir la migración**

Crear `supabase/migrations/20260911000300_leads.sql`:

```sql
-- ============================================================================
-- Leads: el mecanismo por el que un comprador con cuenta contacta sobre una
-- propiedad publicada.
--
-- DOS TABLAS, y ese es el nucleo del diseno. El vendedor ve el mensaje al
-- entrar el lead, pero el correo y el telefono solo cuando lo acepta. Eso no
-- se puede expresar con una politica sobre una sola tabla: RLS es por FILA, no
-- por columna. Y no se deja en manos de un RPC de lectura porque entonces no
-- lo hace cumplir la base -- el vendedor conservaria el SELECT y podria leer
-- la tabla directo por PostgREST.
--
-- Con dos tablas, la condicion "solo tras aceptar" es una politica de una sola
-- expresion sobre leads_contacto, y es falsificable.
-- ============================================================================

CREATE TYPE public.estado_lead AS ENUM ('nuevo','aceptado','descartado');

CREATE TABLE public.leads (
  id              uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  propiedad_id    uuid NOT NULL REFERENCES public.propiedades(id) ON DELETE CASCADE,
  comprador_id    uuid NOT NULL REFERENCES public.perfiles(id)    ON DELETE CASCADE,
  -- Desnormalizado a proposito: la politica de RLS lo necesita. Con solo
  -- propiedad_id habria que subir a propiedades con un EXISTS, y propiedades
  -- tiene DOS politicas permisivas de SELECT que Postgres combina con OR -- una
  -- de ellas estado = 'publicada'. Apoyarse en "RLS ya filtra por dueno" es
  -- exactamente el fallo que costo la Task 11 de SP0.
  vendedor_id     uuid NOT NULL REFERENCES public.perfiles(id)    ON DELETE CASCADE,
  nombre_mostrado text NOT NULL,
  mensaje         text NOT NULL CHECK (char_length(mensaje) BETWEEN 10 AND 1000),
  estado          public.estado_lead NOT NULL DEFAULT 'nuevo',
  creado_en       timestamptz NOT NULL DEFAULT now(),
  respondido_en   timestamptz,
  CONSTRAINT leads_uno_por_comprador_y_propiedad UNIQUE (propiedad_id, comprador_id),
  CONSTRAINT leads_no_a_si_mismo CHECK (comprador_id <> vendedor_id)
);

CREATE TABLE public.leads_contacto (
  lead_id  uuid PRIMARY KEY REFERENCES public.leads(id) ON DELETE CASCADE,
  correo   text NOT NULL,
  telefono text NOT NULL
);

-- La bandeja: los nuevos primero, luego por fecha.
CREATE INDEX leads_bandeja_idx ON public.leads (vendedor_id, estado, creado_en DESC);
-- Un consumidor futuro (SP6) sondea por aqui sin que SP4 sepa que existe.
CREATE INDEX leads_nuevos_idx ON public.leads (creado_en) WHERE estado = 'nuevo';

ALTER TABLE public.leads          ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.leads_contacto ENABLE ROW LEVEL SECURITY;

-- ----------------------------------------------------------------------------
-- Privilegios. Ni anon ni authenticated tienen INSERT en ninguna de las dos:
-- el unico camino de escritura es crear_lead() (20260911000400).
-- El vendedor solo puede tocar la columna `estado`, y de sus propios leads.
-- ----------------------------------------------------------------------------
GRANT SELECT                 ON public.leads          TO authenticated;
GRANT UPDATE (estado)        ON public.leads          TO authenticated;
GRANT SELECT                 ON public.leads_contacto TO authenticated;

CREATE POLICY leads_lectura_vendedor ON public.leads
  FOR SELECT TO authenticated
  USING (vendedor_id = (SELECT auth.uid()));

CREATE POLICY leads_lectura_comprador ON public.leads
  FOR SELECT TO authenticated
  USING (comprador_id = (SELECT auth.uid()));

CREATE POLICY leads_lectura_super_admin ON public.leads
  FOR SELECT TO authenticated
  USING (public.es_super_admin());

-- USING sin WITH CHECK haria las veces de ambos; se declara el WITH CHECK
-- explicito para que la fila resultante tambien tenga que seguir siendo suya.
CREATE POLICY leads_actualizacion_vendedor ON public.leads
  FOR UPDATE TO authenticated
  USING      (vendedor_id = (SELECT auth.uid()))
  WITH CHECK (vendedor_id = (SELECT auth.uid()));

-- El corazon del diseno: el contacto solo es legible cuando el lead padre ya
-- salio de 'nuevo'. Quitar ese AND pone roja la prueba de la Task 3.
CREATE POLICY contacto_lectura_vendedor ON public.leads_contacto
  FOR SELECT TO authenticated
  USING (EXISTS (
    SELECT 1 FROM public.leads l
    WHERE l.id = leads_contacto.lead_id
      AND l.vendedor_id = (SELECT auth.uid())
      AND l.estado <> 'nuevo'
  ));

-- El comprador lee su propio contacto siempre: es suyo.
CREATE POLICY contacto_lectura_comprador ON public.leads_contacto
  FOR SELECT TO authenticated
  USING (EXISTS (
    SELECT 1 FROM public.leads l
    WHERE l.id = leads_contacto.lead_id
      AND l.comprador_id = (SELECT auth.uid())
  ));

CREATE POLICY contacto_lectura_super_admin ON public.leads_contacto
  FOR SELECT TO authenticated
  USING (public.es_super_admin());
```

- [ ] **Step 4: Correr y ver que pasa**

Run (PowerShell): `npx supabase db reset` y luego `npx vitest run tests/rls/leads.test.ts`
Expected: PASS, 3/3.

- [ ] **Step 5: Commit**

```bash
git add supabase/migrations/20260911000300_leads.sql tests/rls/leads.test.ts
git commit -m "feat(leads): modelo de datos en dos tablas con el contacto tras aceptar

El revelado se hace cumplir en la base: leads_contacto solo es legible cuando
el lead padre salio de 'nuevo'. No cabe en una politica sobre una sola tabla
porque RLS es por fila y no por columna, y un RPC de lectura no lo haria
cumplir (el vendedor conservaria el SELECT sobre la tabla)."
```

---

### Task 4: `crear_lead()`, el único camino de escritura

**Files:**
- Create: `supabase/migrations/20260911000400_crear_lead.sql`
- Test: `tests/rls/crear-lead.test.ts`

**Interfaces:**
- Consumes: las tablas de la Task 3, `public.registro_auditoria`.
- Produces: `public.crear_lead(p_propiedad_id uuid, p_telefono text, p_mensaje text) → uuid` (el id del lead creado), invocable por `authenticated` y no por `anon`.

- [ ] **Step 1: Escribir la prueba, que falla**

Crear `tests/rls/crear-lead.test.ts` con seis casos: el camino feliz, propiedad no publicada, lead a la propia propiedad, duplicado, `vendedor_id` derivado y no confiado, y la auditoría. El esqueleto del camino feliz y del derivado:

```ts
it('crea el lead y su contacto, y deriva el vendedor de la propiedad', async () => {
  const { compradorCorreo, password, propiedadId, vendedorId } = await fixtura()
  const cliente = await clienteComo(compradorCorreo, password)

  const { data: leadId, error } = await cliente.rpc('crear_lead', {
    p_propiedad_id: propiedadId,
    p_telefono: '3001234567',
    p_mensaje: 'Me interesa esta propiedad, quisiera visitarla.',
  })
  expect(error).toBeNull()
  expect(leadId).toBeTruthy()

  const admin = clienteAdmin()
  const { data: lead } = await admin.from('leads')
    .select('vendedor_id,estado,nombre_mostrado').eq('id', leadId).single()
  // Derivado de la propiedad, no de lo que mando el cliente -- que no pudo
  // mandar nada: la funcion no acepta vendedor_id como parametro.
  expect(lead?.vendedor_id).toBe(vendedorId)
  expect(lead?.estado).toBe('nuevo')

  const { data: contacto } = await admin.from('leads_contacto')
    .select('correo,telefono').eq('lead_id', leadId).single()
  expect(contacto?.telefono).toBe('3001234567')
  expect(contacto?.correo).toBe(compradorCorreo)
})

it('rechaza el lead sobre una propiedad que no esta publicada', async () => {
  const { compradorCorreo, password, borradorId } = await fixtura()
  const cliente = await clienteComo(compradorCorreo, password)
  const { error } = await cliente.rpc('crear_lead', {
    p_propiedad_id: borradorId, p_telefono: '3001234567',
    p_mensaje: 'Me interesa esta propiedad, quisiera visitarla.',
  })
  // Mensaje propio, distinguible del de "a si mismo": dos comprobaciones que
  // comparten codigo no se pueden mapear a ciegas.
  expect(error?.message).toMatch(/no esta publicada/i)
})

it('rechaza que un vendedor se deje un lead en su propia propiedad', async () => {
  const { vendedorCorreo, password, propiedadId } = await fixtura()
  const cliente = await clienteComo(vendedorCorreo, password)
  const { error } = await cliente.rpc('crear_lead', {
    p_propiedad_id: propiedadId, p_telefono: '3001234567',
    p_mensaje: 'Me interesa esta propiedad, quisiera visitarla.',
  })
  expect(error?.message).toMatch(/tu propia propiedad/i)
})

it('anon no puede invocar crear_lead', async () => {
  const { propiedadId } = await fixtura()
  const { error } = await clienteAnonimo().rpc('crear_lead', {
    p_propiedad_id: propiedadId, p_telefono: '3001234567',
    p_mensaje: 'Me interesa esta propiedad, quisiera visitarla.',
  })
  expect(error?.code).toBe('42501')
})
```

- [ ] **Step 2: Correr y ver que falla**

Run (PowerShell): `npx vitest run tests/rls/crear-lead.test.ts`
Expected: FAIL con `PGRST202` (la función no está en el esquema).

- [ ] **Step 3: Escribir la migración**

Crear `supabase/migrations/20260911000400_crear_lead.sql`:

```sql
-- ============================================================================
-- El unico camino de escritura de un lead.
--
-- Por que una funcion y no un INSERT del cliente: el lead nace en DOS tablas a
-- la vez, y el telefono no va en `leads`. No hay forma de que un solo INSERT
-- del cliente coloque las dos filas, y dos INSERT separados dejarian la puerta
-- a un lead sin contacto.
--
-- Esto NO contradice el rechazo del RPC en la migracion anterior. Alli el
-- problema era de LECTURA: el vendedor conservaba el SELECT y podia saltarse la
-- funcion. Aqui es de ESCRITURA, y sin INSERT concedido no hay camino
-- alternativo -- lo hace cumplir la base, no el codigo de la aplicacion.
--
-- SECURITY DEFINER por necesidad: tiene que leer el correo de auth.users, a la
-- que authenticated no tiene acceso.
-- ============================================================================

CREATE OR REPLACE FUNCTION public.crear_lead(
  p_propiedad_id uuid, p_telefono text, p_mensaje text
) RETURNS uuid LANGUAGE plpgsql SECURITY DEFINER SET search_path = '' AS $$
DECLARE
  v_comprador uuid := auth.uid();
  v_vendedor  uuid;
  v_estado    public.estado_propiedad;
  v_nombre    text;
  v_correo    text;
  v_lead      uuid;
BEGIN
  IF v_comprador IS NULL THEN
    RAISE EXCEPTION 'Hay que iniciar sesion para contactar' USING ERRCODE = '42501';
  END IF;

  -- Se leen de la propiedad, no se creen al cliente. La funcion no acepta
  -- vendedor_id como parametro: no hay nada que falsificar.
  SELECT vendedor_id, estado INTO v_vendedor, v_estado
  FROM public.propiedades WHERE id = p_propiedad_id;

  IF v_vendedor IS NULL THEN
    RAISE EXCEPTION 'La propiedad no existe' USING ERRCODE = 'P0002';
  END IF;

  -- Mensajes DISTINTOS a proposito: mapear a ciegas un codigo compartido por
  -- dos comprobaciones es un defecto que este proyecto ya cometio.
  IF v_estado <> 'publicada' THEN
    RAISE EXCEPTION 'La propiedad no esta publicada' USING ERRCODE = 'P0001';
  END IF;

  IF v_vendedor = v_comprador THEN
    RAISE EXCEPTION 'No puedes contactar sobre tu propia propiedad' USING ERRCODE = 'P0001';
  END IF;

  SELECT nombre INTO v_nombre FROM public.perfiles WHERE id = v_comprador;
  SELECT email  INTO v_correo FROM auth.users      WHERE id = v_comprador;

  -- Las dos filas en la MISMA transaccion: un lead no existe sin su contacto.
  INSERT INTO public.leads (propiedad_id, comprador_id, vendedor_id, nombre_mostrado, mensaje)
  VALUES (p_propiedad_id, v_comprador, v_vendedor, v_nombre, p_mensaje)
  RETURNING id INTO v_lead;

  INSERT INTO public.leads_contacto (lead_id, correo, telefono)
  VALUES (v_lead, v_correo, p_telefono);

  -- La auditoria se escribe AQUI y no en el server action, por el mismo motivo
  -- que documenta 20260831000700: tiene que cubrir el evento, no el formulario.
  INSERT INTO public.registro_auditoria (actor_id, accion, entidad, entidad_id, metadatos)
  VALUES (v_comprador, 'lead_capturado', 'lead', v_lead,
          jsonb_build_object('propiedad_id', p_propiedad_id, 'vendedor_id', v_vendedor));

  RETURN v_lead;
END $$;

REVOKE EXECUTE ON FUNCTION public.crear_lead(uuid, text, text) FROM PUBLIC, anon;
GRANT  EXECUTE ON FUNCTION public.crear_lead(uuid, text, text) TO authenticated, service_role;
```

- [ ] **Step 4: Correr y ver que pasa**

Run (PowerShell): `npx supabase db reset` y luego `npx vitest run tests/rls/crear-lead.test.ts`
Expected: PASS.

- [ ] **Step 5: Falsificar la derivación del vendedor**

Cambiar `v_vendedor` por `v_comprador` en el `INSERT INTO public.leads`, resetear, correr.
Expected: FAIL en «deriva el vendedor de la propiedad» — y también en el `CHECK leads_no_a_si_mismo`, que es la segunda red.

Restaurar, resetear, correr. Expected: PASS. Pegar las dos salidas.

- [ ] **Step 6: Commit**

```bash
git add supabase/migrations/20260911000400_crear_lead.sql tests/rls/crear-lead.test.ts
git commit -m "feat(leads): crear_lead() como unico camino de escritura

Las dos filas en una transaccion, y las tres reglas (propiedad publicada, no a
si mismo, sesion iniciada) dentro de la funcion. El vendedor_id se DERIVA de la
propiedad: no es un parametro, asi que no hay nada que falsificar."
```

---

### Task 5: Transición de estado y su auditoría

**Files:**
- Create: `supabase/migrations/20260911000500_transicion_lead.sql`
- Test: `tests/rls/transicion-lead.test.ts`

**Interfaces:**
- Consumes: las tablas de la Task 3.
- Produces: trigger `leads_validar_transicion` sobre `public.leads`, que fija `respondido_en` y escribe `lead_aceptado` / `lead_descartado` en `registro_auditoria`.

- [ ] **Step 1: Escribir la prueba, que falla**

Crear `tests/rls/transicion-lead.test.ts`:

```ts
it('el vendedor acepta y entonces ve el contacto; antes no', async () => {
  const { vendedorCorreo, password, leadId } = await fixturaConLead()
  const vendedor = await clienteComo(vendedorCorreo, password)

  // Antes de aceptar: cero filas. No es un error de permisos, es que la
  // politica no deja ver la fila -- por eso se asserta la lista vacia.
  const antes = await vendedor.from('leads_contacto').select('correo').eq('lead_id', leadId)
  expect(antes.error).toBeNull()
  expect(antes.data ?? []).toEqual([])

  const aceptar = await vendedor.from('leads')
    .update({ estado: 'aceptado' }).eq('id', leadId).select('id')
  expect(aceptar.error).toBeNull()
  // .select() encadenado: un UPDATE de PostgREST que afecta CERO filas devuelve
  // error null. Sin esto, un fallo pasaria por exito.
  expect(aceptar.data?.length).toBe(1)

  const despues = await vendedor.from('leads_contacto').select('correo').eq('lead_id', leadId)
  expect(despues.data?.length).toBe(1)
})

it('un lead que ya salio de nuevo no vuelve a cambiar', async () => {
  const { vendedorCorreo, password, leadId } = await fixturaConLead()
  const vendedor = await clienteComo(vendedorCorreo, password)
  await vendedor.from('leads').update({ estado: 'descartado' }).eq('id', leadId).select('id')

  const segundo = await vendedor.from('leads')
    .update({ estado: 'aceptado' }).eq('id', leadId).select('id')
  expect(segundo.error?.message).toMatch(/ya fue respondido/i)
})

it('un vendedor ajeno no ve el lead ni su contacto en ningun estado', async () => {
  const { leadId } = await fixturaConLead()
  const ajeno = await sesionVendedor()

  expect((await ajeno.from('leads').select('id').eq('id', leadId)).data ?? []).toEqual([])
  expect((await ajeno.from('leads_contacto').select('correo').eq('lead_id', leadId)).data ?? [])
    .toEqual([])

  // Caso positivo: el lead existe de verdad, visto con service_role.
  const { data } = await clienteAdmin().from('leads').select('id').eq('id', leadId).single()
  expect(data?.id).toBe(leadId)
})
```

- [ ] **Step 2: Correr y ver que falla**

Run (PowerShell): `npx vitest run tests/rls/transicion-lead.test.ts`
Expected: FAIL en «no vuelve a cambiar» — sin trigger, el segundo `UPDATE` funciona.

- [ ] **Step 3: Escribir la migración**

Crear `supabase/migrations/20260911000500_transicion_lead.sql`:

```sql
-- ============================================================================
-- Transiciones de estado de un lead: nuevo -> aceptado | descartado, y nunca
-- la vuelta.
--
-- Va en un trigger y no en el server action porque el vendedor SI escribe
-- directo aqui: tiene UPDATE (estado) sobre sus propios leads por RLS. La
-- interfaz no es el unico camino.
-- ============================================================================

CREATE OR REPLACE FUNCTION public.validar_transicion_lead() RETURNS trigger
LANGUAGE plpgsql SECURITY DEFINER SET search_path = '' AS $$
BEGIN
  IF NEW.estado IS NOT DISTINCT FROM OLD.estado THEN
    RETURN NEW;
  END IF;

  IF OLD.estado <> 'nuevo' THEN
    RAISE EXCEPTION 'Este lead ya fue respondido' USING ERRCODE = 'P0001';
  END IF;

  NEW.respondido_en := now();

  INSERT INTO public.registro_auditoria (actor_id, accion, entidad, entidad_id, metadatos)
  VALUES (
    auth.uid(),
    CASE NEW.estado WHEN 'aceptado' THEN 'lead_aceptado' ELSE 'lead_descartado' END,
    'lead', NEW.id,
    jsonb_build_object('propiedad_id', NEW.propiedad_id)
  );

  RETURN NEW;
END $$;

CREATE TRIGGER leads_validar_transicion
  BEFORE UPDATE ON public.leads
  FOR EACH ROW EXECUTE FUNCTION public.validar_transicion_lead();

REVOKE EXECUTE ON FUNCTION public.validar_transicion_lead() FROM PUBLIC, anon, authenticated;
GRANT  EXECUTE ON FUNCTION public.validar_transicion_lead() TO service_role;
```

- [ ] **Step 4: Correr y ver que pasa**

Run (PowerShell): `npx supabase db reset` y luego `npx vitest run tests/rls/transicion-lead.test.ts`
Expected: PASS.

- [ ] **Step 5: Falsificar la política del revelado**

En `20260911000300_leads.sql`, quitar `AND l.estado <> 'nuevo'` de `contacto_lectura_vendedor`. Resetear. Correr.
Expected: FAIL en «antes de aceptar: cero filas» — recibe una fila.

Restaurar, resetear, correr. Expected: PASS. **Esta es la falsificación más importante de SP4**: es la que demuestra que el revelado lo hace cumplir la base.

- [ ] **Step 6: Verificar la suite completa**

Run (PowerShell): `npm run test:rls`
Expected: PASS. El total debe ser 133 + las de las Tasks 3, 4 y 5.

- [ ] **Step 7: Commit**

```bash
git add supabase/migrations/20260911000500_transicion_lead.sql tests/rls/transicion-lead.test.ts
git commit -m "feat(leads): transicion de estado con trigger, y auditoria de las dos

En trigger y no en el server action porque el vendedor escribe directo: tiene
UPDATE (estado) por RLS y la interfaz no es el unico camino.

Falsificada la politica del revelado: sin el 'AND l.estado <> nuevo', el
vendedor ve el contacto antes de aceptar y la prueba se pone roja."
```

---

### Task 6: Validación y mensajes

**Files:**
- Modify: `src/lib/validacion/esquemas.ts`
- Modify: `src/lib/errores/mapear.ts`
- Create: `src/lib/navegacion/volver.ts`
- Test: `tests/unit/esquema-lead.test.ts`, `tests/unit/volver.test.ts`

**Interfaces:**
- Consumes: nada.
- Produces: `esquemaLead` (Zod) con `{ telefono: string; mensaje: string }`; `rutaDeRetorno(valor: string | null): string` en `src/lib/navegacion/volver.ts`; y en `mapear.ts` las constantes `MENSAJE_LEAD_DUPLICADO`, `MENSAJE_LEAD_NO_PUBLICADA`, `MENSAJE_LEAD_PROPIA`, `MENSAJE_LEAD_YA_RESPONDIDO`.

- [ ] **Step 1: Escribir las pruebas del retorno, que fallan**

Crear `tests/unit/volver.test.ts`:

```ts
import { describe, it, expect } from 'vitest'
import { rutaDeRetorno } from '@/lib/navegacion/volver'

describe('rutaDeRetorno', () => {
  it('acepta una ruta interna', () => {
    expect(rutaDeRetorno('/riomar/casa-abc')).toBe('/riomar/casa-abc')
  })

  // Los tres que importan: cada uno es un redirect abierto si pasa.
  it('rechaza una URL absoluta externa', () => {
    expect(rutaDeRetorno('https://malo.test/phishing')).toBe('/')
  })

  it('rechaza el doble slash, que el navegador lee como protocolo relativo', () => {
    expect(rutaDeRetorno('//malo.test/phishing')).toBe('/')
  })

  it('rechaza la barra invertida, que algunos navegadores normalizan a /', () => {
    expect(rutaDeRetorno('/\\malo.test')).toBe('/')
    expect(rutaDeRetorno('\\\\malo.test')).toBe('/')
  })

  it('rechaza null y la cadena vacia', () => {
    expect(rutaDeRetorno(null)).toBe('/')
    expect(rutaDeRetorno('')).toBe('/')
  })
})
```

- [ ] **Step 2: Correr y ver que falla**

Run (PowerShell): `npx vitest run tests/unit/volver.test.ts`
Expected: FAIL, el módulo no existe.

- [ ] **Step 3: Escribir el módulo**

Crear `src/lib/navegacion/volver.ts`:

```ts
/**
 * Ruta a la que volver tras iniciar sesion, saneada.
 *
 * El parametro `volver` viaja en la URL, asi que lo controla quien mande el
 * enlace. Sin sanear es un redirect abierto: basta con `?volver=https://malo`
 * para que el portal lleve al usuario a un sitio ajeno justo despues de que
 * escriba su contrasena.
 *
 * Se valida por forma y no con `new URL()` porque lo unico aceptable es una
 * ruta interna, y eso se decide mirando como empieza. Ante cualquier duda, la
 * portada: degradar a un destino seguro nunca rompe nada.
 */
export function rutaDeRetorno(valor: string | null): string {
  if (!valor) return '/'
  if (!valor.startsWith('/')) return '/'
  // `//malo.test` es un protocolo relativo: el navegador lo resuelve como
  // https://malo.test. Y la barra invertida la normalizan algunos navegadores
  // a `/`, asi que `/\malo.test` acabaria fuera igual.
  if (valor.startsWith('//') || valor.includes('\\')) return '/'
  return valor
}
```

- [ ] **Step 4: Correr y ver que pasa**

Run (PowerShell): `npx vitest run tests/unit/volver.test.ts`
Expected: PASS, 6/6.

- [ ] **Step 5: Escribir la prueba del esquema y el esquema**

Crear `tests/unit/esquema-lead.test.ts`:

```ts
import { describe, it, expect } from 'vitest'
import { esquemaLead } from '@/lib/validacion/esquemas'

const valido = { mensaje: 'Me interesa esta propiedad, quisiera visitarla.', telefono: '3001234567' }

describe('esquemaLead', () => {
  it('acepta un lead valido', () => {
    expect(esquemaLead.safeParse(valido).success).toBe(true)
  })

  it('rechaza un mensaje de menos de 10 caracteres', () => {
    expect(esquemaLead.safeParse({ ...valido, mensaje: 'hola' }).success).toBe(false)
  })

  // El limite alto tiene que coincidir con el CHECK de la tabla. Si el esquema
  // dejara pasar 1001, el usuario recibiria un error de base de datos en vez de
  // uno del formulario.
  it('rechaza un mensaje de mas de 1000 caracteres', () => {
    expect(esquemaLead.safeParse({ ...valido, mensaje: 'x'.repeat(1001) }).success).toBe(false)
    expect(esquemaLead.safeParse({ ...valido, mensaje: 'x'.repeat(1000) }).success).toBe(true)
  })

  // FormData devuelve cadena vacia, no undefined, para un campo vacio: por eso
  // se prueba '' y no la ausencia del campo.
  it('rechaza el telefono vacio', () => {
    expect(esquemaLead.safeParse({ ...valido, telefono: '' }).success).toBe(false)
    expect(esquemaLead.safeParse({ ...valido, telefono: '   ' }).success).toBe(false)
  })
})
```

Después, en `src/lib/validacion/esquemas.ts`:

```ts
export const esquemaLead = z.object({
  // El limite alto coincide con el CHECK de la tabla: si divergen, el usuario
  // recibiria un error de base de datos en vez de uno del formulario.
  mensaje: z.string().trim()
    .min(10, 'Cuentale al vendedor que te interesa, con al menos 10 caracteres.')
    .max(1000, 'El mensaje no puede pasar de 1000 caracteres.'),
  // Obligatorio aunque `perfiles.telefono` sea nullable: un lead sin telefono
  // no le sirve de nada al vendedor.
  telefono: z.string().trim()
    .min(7, 'Escribe un telefono de contacto.')
    .max(20, 'Ese telefono es demasiado largo.'),
})

export type DatosLead = z.infer<typeof esquemaLead>
```

- [ ] **Step 6: Añadir los mensajes de error**

En `src/lib/errores/mapear.ts`:

```ts
export const MENSAJE_LEAD_DUPLICADO = 'Ya contactaste sobre esta propiedad.'
export const MENSAJE_LEAD_NO_PUBLICADA = 'Esta propiedad ya no esta disponible.'
export const MENSAJE_LEAD_PROPIA = 'No puedes contactar sobre tu propia propiedad.'
export const MENSAJE_LEAD_YA_RESPONDIDO = 'Este lead ya fue respondido.'
```

No se añade ninguna rama a `mapearError()`: las tres causas de lead se distinguen **antes** de llegar ahí, porque `crear_lead()` levanta mensajes distintos y el server action los traduce. La regla del módulo —el mensaje nunca se construye a partir del error original— sigue intacta.

- [ ] **Step 7: Verificar y commitear**

Run (PowerShell): `npm run test:unit` → PASS. `npx tsc --noEmit` → sin salida.

```bash
git add src/lib/validacion/esquemas.ts src/lib/errores/mapear.ts src/lib/navegacion/volver.ts tests/unit/
git commit -m "feat(leads): validacion del lead y saneado de la ruta de retorno

rutaDeRetorno rechaza absolutas, protocolo relativo (//) y barra invertida: los
tres son redirect abierto si pasan."
```

---

### Task 7: El formulario en la ficha

**Files:**
- Create: `src/app/[barrio]/[slug]/formulario-lead.tsx`
- Create: `src/app/[barrio]/[slug]/acciones.ts`
- Modify: `src/app/[barrio]/[slug]/page.tsx`
- Test: `tests/unit/formulario-lead.test.ts`

**Interfaces:**
- Consumes: `esquemaLead`, `rutaDeRetorno`, las constantes de mensaje de la Task 6; `crear_lead` de la Task 4; `sesionActual()` de `src/lib/auth/sesion.ts`.
- Produces: componente `<FormularioLead propiedadId={string} rutaFicha={string} esDelVendedor={boolean} />` y server action `enviarLead(estadoPrevio: EstadoLead, formData: FormData): Promise<EstadoLead>` con `EstadoLead = { error?: string; enviado?: boolean }`.

- [ ] **Step 1: Escribir el server action**

Crear `src/app/[barrio]/[slug]/acciones.ts`:

```ts
'use server'

import { crearClienteServidor } from '@/lib/supabase/cliente-servidor'
import { esquemaLead } from '@/lib/validacion/esquemas'
import {
  MENSAJE_GENERICO, MENSAJE_LEAD_DUPLICADO,
  MENSAJE_LEAD_NO_PUBLICADA, MENSAJE_LEAD_PROPIA,
} from '@/lib/errores/mapear'

export interface EstadoLead {
  error?: string
  enviado?: boolean
}

export async function enviarLead(_previo: EstadoLead, formData: FormData): Promise<EstadoLead> {
  const analisis = esquemaLead.safeParse({
    mensaje: formData.get('mensaje'),
    telefono: formData.get('telefono'),
  })
  if (!analisis.success) {
    return { error: analisis.error.issues[0]?.message ?? MENSAJE_GENERICO }
  }

  const propiedadId = formData.get('propiedad_id')
  if (typeof propiedadId !== 'string') return { error: MENSAJE_GENERICO }

  const { error } = await (await crearClienteServidor()).rpc('crear_lead', {
    p_propiedad_id: propiedadId,
    p_telefono: analisis.data.telefono,
    p_mensaje: analisis.data.mensaje,
  })

  if (!error) return { enviado: true }

  // Las tres causas se distinguen AQUI y no en mapearError: crear_lead levanta
  // mensajes distintos a proposito, y mapear a ciegas un codigo compartido por
  // dos comprobaciones es un defecto que este proyecto ya cometio.
  if (error.code === '23505') return { error: MENSAJE_LEAD_DUPLICADO }
  if (/no esta publicada/i.test(error.message)) return { error: MENSAJE_LEAD_NO_PUBLICADA }
  if (/tu propia propiedad/i.test(error.message)) return { error: MENSAJE_LEAD_PROPIA }
  return { error: MENSAJE_GENERICO }
}
```

- [ ] **Step 2: Escribir el componente**

Crear `src/app/[barrio]/[slug]/formulario-lead.tsx`. Es un componente de **cliente** (necesita `useActionState` para enseñar el error sin recargar), y esa es la única pieza de cliente de SP4 — no lleva `<script>` propio, así que la CSP con `strict-dynamic` lo admite igual que al resto de la aplicación.

```tsx
'use client'

import { useActionState } from 'react'
import { enviarLead, type EstadoLead } from './acciones'

const CAMPO = 'block w-full rounded-sm border border-linea bg-superficie px-3 py-2 text-tinta'

export function FormularioLead(
  { propiedadId, telefonoPrevio }: { propiedadId: string; telefonoPrevio: string },
) {
  const [estado, accion, enviando] = useActionState<EstadoLead, FormData>(enviarLead, {})

  if (estado.enviado) {
    return (
      <p className="rounded-md border border-exito bg-exito-suave p-4 text-exito">
        Tu mensaje se envio. El vendedor vera tus datos de contacto cuando lo acepte.
      </p>
    )
  }

  return (
    <form action={accion} className="rounded-md border border-linea bg-superficie p-6">
      <h2 className="text-xl font-semibold text-tinta">Contactar al vendedor</h2>
      <input type="hidden" name="propiedad_id" value={propiedadId} />

      <label className="mt-4 block text-sm text-tinta-suave">
        Telefono
        <input className={`${CAMPO} mt-1`} name="telefono" defaultValue={telefonoPrevio} required />
      </label>

      <label className="mt-4 block text-sm text-tinta-suave">
        Mensaje
        <textarea className={`${CAMPO} mt-1`} name="mensaje" rows={4} required
          placeholder="Cuentale al vendedor que te interesa." />
      </label>

      {estado.error && <p className="mt-3 text-sm text-peligro">{estado.error}</p>}

      <button type="submit" disabled={enviando}
        className="mt-4 cursor-pointer rounded-sm bg-marca px-5 py-2 font-medium text-marca-contraste hover:bg-marca-fuerte disabled:opacity-60">
        {enviando ? 'Enviando...' : 'Enviar mensaje'}
      </button>
    </form>
  )
}
```

- [ ] **Step 3: Montarlo en la ficha**

En `src/app/[barrio]/[slug]/page.tsx`, al final del `<main>`, antes de cerrar. La ficha ya resuelve `p` y `barrio`; hay que añadir la sesión y el estado previo:

```tsx
// La ficha es publica y esta cacheada por SP1; esta parte depende de la
// sesion, asi que se resuelve en cada peticion. El layout raiz ya declara
// force-dynamic, de modo que no cuesta nada extra.
const sesion = await sesionActual()
const rutaFicha = `/${barrio.slug}/${p.slug}`

{!sesion.hayUsuario ? (
  <p className="mt-8 rounded-md border border-linea bg-superficie p-6">
    <Link href={`/login?volver=${encodeURIComponent(rutaFicha)}`}
      className="font-medium text-marca hover:underline">
      Entra o crea cuenta para contactar
    </Link>{' '}
    al vendedor de esta propiedad.
  </p>
) : yaContacto ? (
  <p className="mt-8 rounded-md border border-linea bg-superficie p-6 text-tinta-suave">
    Ya contactaste sobre esta propiedad.
  </p>
) : esDelVendedor ? null : (
  <div className="mt-8">
    <FormularioLead propiedadId={p.id} telefonoPrevio={telefonoPrevio} />
  </div>
)}
```

**La ficha necesita `p.id` y `p.vendedor_id`**, que hoy no están en el `select` de `cargarFicha` (línea 8 de `page.tsx`) — hay que añadirlos a la lista explícita de columnas.

Las tres variables que usa el bloque de arriba:

```tsx
const sesion = await sesionActual()
const rutaFicha = `/${barrio.slug}/${p.slug}`

// Las dos lecturas solo tienen sentido con sesion, y se saltan sin ella.
let yaContacto = false
let telefonoPrevio = ''
let esDelVendedor = false

if (sesion.hayUsuario) {
  const db = await crearClienteServidor()
  // RLS ya limita `leads` al propio comprador (politica leads_lectura_comprador),
  // asi que no hace falta filtrar por comprador_id: hacerlo duplicaria la regla.
  const [{ data: previo }, { data: perfil }] = await Promise.all([
    db.from('leads').select('id').eq('propiedad_id', p.id).maybeSingle(),
    db.from('perfiles').select('telefono').maybeSingle(),
  ])
  yaContacto = previo !== null
  telefonoPrevio = perfil?.telefono ?? ''
  esDelVendedor = sesion.idUsuario === p.vendedor_id
}
```

Comprobar la propiedad exacta de `sesionActual()` que devuelve el id (`src/lib/auth/sesion.ts`) antes de escribir `sesion.idUsuario`: si el tipo `Sesion` no la expone, se añade ahí y no se deriva del token en la ficha.

- [ ] **Step 4: Aplicar el retorno en el login**

En `src/app/(auth)/login/acciones.ts`, el `redirect` final pasa por `rutaDeRetorno`:

```ts
const volver = rutaDeRetorno(formData.get('volver') as string | null)
redirect(volver !== '/' ? volver : rutaDePanel(rolDesdeToken(data.session.access_token)))
```

Y el formulario de login lleva el `volver` en un campo oculto, leído de `searchParams`.

- [ ] **Step 5: Verificar en el navegador**

Run (PowerShell): asegurarse de que el servidor de desarrollo está arriba, y comprobar a mano: ficha sin sesión enseña el enlace; con sesión de comprador enseña el formulario; tras enviar, el mensaje de confirmación; al recargar, «Ya contactaste».

- [ ] **Step 6: Verificar y commitear**

Run: `npm run test:unit`, `npm run build`, `npx tsc --noEmit`, `npm run lint`. Todo en verde.

```bash
git add "src/app/[barrio]/[slug]/" "src/app/(auth)/login/" tests/unit/
git commit -m "feat(leads): formulario de contacto en la ficha, con retorno al login

Sin sesion se ofrece un enlace y no un formulario, con ?volver saneado por
rutaDeRetorno. Al vendedor de la propia propiedad no se le ofrece el formulario:
crear_lead lo rechazaria igual, pero no tiene sentido ofrecer lo que va a fallar."
```

---

### Task 8: La bandeja del vendedor

**Files:**
- Create: `src/lib/leads/consultas.ts`
- Create: `src/app/(vendedor)/panel/leads/page.tsx`
- Create: `src/app/(vendedor)/panel/leads/acciones.ts`
- Create: `src/app/(vendedor)/panel/leads/acciones-fila.tsx`
- Modify: `src/app/(vendedor)/panel/page.tsx`
- Test: `tests/unit/consultas-leads.test.ts`

**Interfaces:**
- Consumes: las tablas de la Task 3, el trigger de la Task 5, `MENSAJE_LEAD_YA_RESPONDIDO`.
- Produces: `listarLeadsDelVendedor(cliente): Promise<LeadDeBandeja[]>` y `contarLeadsNuevos(cliente): Promise<number>` en `src/lib/leads/consultas.ts`; server actions `aceptarLead(formData)` y `descartarLead(formData)`.

- [ ] **Step 1: Escribir las consultas**

Crear `src/lib/leads/consultas.ts`:

```ts
import type { SupabaseClient } from '@supabase/supabase-js'

export interface LeadDeBandeja {
  id: string
  nombre_mostrado: string
  mensaje: string
  estado: 'nuevo' | 'aceptado' | 'descartado'
  creado_en: string
  propiedades: { titulo: string; slug: string } | null
  leads_contacto: { correo: string; telefono: string } | null
}

/**
 * La bandeja. `leads_contacto` se pide SIEMPRE, sin condicion en el cliente: la
 * politica de RLS decide si vuelve o no segun el estado del lead. Poner aqui un
 * "solo si aceptado" duplicaria la regla en dos sitios, y el sitio que manda es
 * la base -- si algun dia divergen, gana la base y este filtro solo serviria
 * para ocultar un fallo.
 */
export async function listarLeadsDelVendedor(cliente: SupabaseClient): Promise<LeadDeBandeja[]> {
  const { data, error } = await cliente
    .from('leads')
    .select('id,nombre_mostrado,mensaje,estado,creado_en,propiedades(titulo,slug),leads_contacto(correo,telefono)')
    .order('estado', { ascending: true })
    .order('creado_en', { ascending: false })
  if (error) throw new Error('No se pudo cargar la bandeja de leads')
  return (data ?? []) as unknown as LeadDeBandeja[]
}

export async function contarLeadsNuevos(cliente: SupabaseClient): Promise<number> {
  const { count, error } = await cliente
    .from('leads').select('id', { count: 'exact', head: true }).eq('estado', 'nuevo')
  // Un contador que falla no debe tumbar el panel entero: se degrada a cero.
  if (error) return 0
  return count ?? 0
}
```

- [ ] **Step 2: Escribir los server actions**

Crear `src/app/(vendedor)/panel/leads/acciones.ts`:

```ts
'use server'

import { revalidatePath } from 'next/cache'
import { crearClienteServidor } from '@/lib/supabase/cliente-servidor'
import { MENSAJE_GENERICO, MENSAJE_LEAD_YA_RESPONDIDO } from '@/lib/errores/mapear'

async function cambiarEstadoLead(id: string, estado: 'aceptado' | 'descartado') {
  const { data, error } = await (await crearClienteServidor())
    .from('leads').update({ estado }).eq('id', id).select('id')

  // .select() encadenado a proposito: un UPDATE de PostgREST que afecta CERO
  // filas devuelve error null. Sin el, un lead ajeno o ya respondido pasaria
  // por exito.
  if (error) {
    return { error: /ya fue respondido/i.test(error.message) ? MENSAJE_LEAD_YA_RESPONDIDO : MENSAJE_GENERICO }
  }
  if (!data || data.length === 0) return { error: MENSAJE_LEAD_YA_RESPONDIDO }

  revalidatePath('/panel/leads')
  revalidatePath('/panel')
  return {}
}

export async function aceptarLead(formData: FormData) {
  const id = formData.get('id')
  if (typeof id !== 'string') return { error: MENSAJE_GENERICO }
  return cambiarEstadoLead(id, 'aceptado')
}

export async function descartarLead(formData: FormData) {
  const id = formData.get('id')
  if (typeof id !== 'string') return { error: MENSAJE_GENERICO }
  return cambiarEstadoLead(id, 'descartado')
}
```

- [ ] **Step 3: Escribir los botones como componente de cliente**

`<form action={aceptarLead}>` a secas **descarta el valor devuelto**: el error nunca llegaría a la pantalla. Hace falta `useActionState`, y eso obliga a un componente de cliente para los dos botones. La página sigue siendo de servidor.

Crear `src/app/(vendedor)/panel/leads/acciones-fila.tsx`:

```tsx
'use client'

import { useActionState } from 'react'
import { aceptarLead, descartarLead } from './acciones'

type Resultado = { error?: string }

export function AccionesLead({ id }: { id: string }) {
  const [estado, accion, ocupado] = useActionState<Resultado, FormData>(
    async (_previo, formData) =>
      formData.get('decision') === 'aceptar'
        ? await aceptarLead(formData)
        : await descartarLead(formData),
    {},
  )

  return (
    <form action={accion} className="mt-3 flex items-center gap-3">
      <input type="hidden" name="id" value={id} />
      <button type="submit" name="decision" value="aceptar" disabled={ocupado}
        className="cursor-pointer rounded-sm bg-marca px-4 py-1.5 text-sm font-medium text-marca-contraste hover:bg-marca-fuerte disabled:opacity-60">
        Aceptar
      </button>
      <button type="submit" name="decision" value="descartar" disabled={ocupado}
        className="cursor-pointer rounded-sm border border-linea px-4 py-1.5 text-sm text-tinta-suave hover:border-peligro hover:text-peligro disabled:opacity-60">
        Descartar
      </button>
      {estado.error && <span className="text-sm text-peligro">{estado.error}</span>}
    </form>
  )
}
```

- [ ] **Step 4: Escribir la página**

Crear `src/app/(vendedor)/panel/leads/page.tsx`, de servidor:

```tsx
export const metadata: Metadata = {
  title: 'Leads | Portal Inmobiliario',
  robots: { index: false, follow: false },
}

export default async function PaginaLeads() {
  const leads = await listarLeadsDelVendedor(await crearClienteServidor())

  return <main className="mx-auto w-full max-w-3xl px-6 py-10">
    <h1 className="text-3xl font-semibold text-tinta">Mensajes recibidos</h1>
    {leads.length === 0 ? (
      <p className="mt-8 rounded-md border border-linea bg-superficie p-8 text-center text-tinta-suave">
        Todavia no has recibido mensajes sobre tus propiedades.
      </p>
    ) : <ul className="mt-8 space-y-4">{leads.map((lead) => (
      <li key={lead.id} className="rounded-md border border-linea bg-superficie p-5">
        <p className="text-sm text-tinta-tenue">{lead.propiedades?.titulo}</p>
        <p className="mt-1 font-medium text-tinta">{lead.nombre_mostrado}</p>
        <p className="mt-2 whitespace-pre-wrap text-tinta-suave">{lead.mensaje}</p>

        {/* Se pinta si la consulta lo devolvio, y quien decide eso es la
            politica de RLS segun el estado del lead -- no un if de negocio. */}
        {lead.leads_contacto && (
          <p className="cifra mt-3 text-sm text-tinta">
            {lead.leads_contacto.correo} · {lead.leads_contacto.telefono}
          </p>
        )}

        {lead.estado === 'nuevo'
          ? <AccionesLead id={lead.id} />
          : <p className="mt-3 text-sm text-tinta-tenue">{ETIQUETA_ESTADO[lead.estado]}</p>}
      </li>
    ))}</ul>}
  </main>
}
```

Con `const ETIQUETA_ESTADO = { aceptado: 'Aceptado', descartado: 'Descartado' } as const` en el mismo fichero.

- [ ] **Step 5: El contador en el panel**

En `src/app/(vendedor)/panel/page.tsx`, junto al enlace de publicar:

```tsx
const nuevos = await contarLeadsNuevos(db)

<Link href="/panel/leads" className="text-marca hover:underline">
  Mensajes recibidos{nuevos > 0 ? ` (${nuevos})` : ''}
</Link>
```

- [ ] **Step 6: Verificar a mano y commitear**

Comprobar el recorrido completo en el navegador: el vendedor ve el lead sin contacto, acepta, aparece el contacto, y el botón ya no permite volver a cambiarlo.

Run: `npm run test:unit`, `npm run build`, `npx tsc --noEmit`, `npm run lint`.

```bash
git add src/lib/leads/ "src/app/(vendedor)/panel/" tests/unit/
git commit -m "feat(leads): bandeja del vendedor con aceptar y descartar

El contacto se pide siempre en la consulta y es RLS quien decide si vuelve.
Duplicar aqui el 'solo si aceptado' pondria la regla en dos sitios y el de
aqui solo serviria para ocultar un fallo del que manda."
```

---

### Task 9: E2E y documentación

**Files:**
- Create: `tests/e2e/leads.spec.ts`
- Modify: `docs/DESPLIEGUE.md`
- Modify: `docs/ESTADO-Y-GUIA.md`

**Interfaces:**
- Consumes: todo lo anterior.
- Produces: nada que otra tarea consuma.

- [ ] **Step 1: Escribir el E2E**

Crear `tests/e2e/leads.spec.ts`. El recorrido: un comprador con cuenta abre una ficha publicada, envía el lead, y el vendedor lo ve en su bandeja **sin contacto**; acepta, y aparece.

La aserción que importa:

```ts
// Contra el HTML SERVIDO, no contra lo que se vea en pantalla: la prueba tiene
// que fallar tambien si el correo llega al navegador y solo lo esconde el CSS.
const htmlAntes = await page.content()
expect(htmlAntes).not.toContain(correoComprador)

await page.getByRole('button', { name: 'Aceptar' }).first().click()

await expect(page.getByText(correoComprador)).toBeVisible()
```

Usar cuentas **efímeras** con `randomUUID()` en el correo, nunca las del seed: `tests/rls/ayudantes.ts` documenta en `sesionVendedor()` por qué una cuenta compartida hace que una de cada tres corridas caiga en rojo bajo paralelismo.

- [ ] **Step 2: Correr la suite E2E completa, varias veces**

Run (PowerShell): `npm run test:e2e`
Expected: PASS, 15/15 (14 + 1).

Correrla **tres veces seguidas**. Una corrida verde no prueba nada cuando el fallo posible es una carrera — en este proyecto ya pasó: un agente reportó 14/14 dos veces y la suite fallaba 2 de cada 3.

- [ ] **Step 3: Documentar la variable nueva**

En `docs/DESPLIEGUE.md`, la lista de variables de Vercel pasa de cuatro a cinco:

```bash
vercel env add IP_CABECERA_CONFIABLE production
```

Con el aviso, en la sección «Lo que rompe en silencio si nadie lo configura»: en Vercel el valor es `x-vercel-forwarded-for`, y **sin ella el registro queda cerrado** — no degradado, cerrado. Es deliberado.

- [ ] **Step 4: Actualizar el estado del proyecto**

En `docs/ESTADO-Y-GUIA.md`, mover SP4 a construido, anotar los totales nuevos de las suites, y dejar dicho que `lead_capturado` es observable como fila (`estado = 'nuevo'`) para quien construya SP6, sin que haga falta tocar SP4.

- [ ] **Step 5: Verificación final completa**

Run (PowerShell), en este orden:

```
npx supabase db reset
npm run test:unit
npm run test:rls
npm run test:e2e
npm run build
npm run verificar:render
npx tsc --noEmit
npm run lint
```

Pegar los totales **reales**. Si algo queda en rojo, decirlo con la salida.

- [ ] **Step 6: Commit**

```bash
git add tests/e2e/leads.spec.ts docs/
git commit -m "test(leads): recorrido E2E completo, y documentar IP_CABECERA_CONFIABLE

El E2E asserta contra el HTML SERVIDO que el correo del comprador no esta en la
pagina antes de aceptar: asi falla tambien si llega al navegador y solo lo
esconde el CSS."
```

---

## Criterios de aceptación del spec, y dónde se cumplen

| # | Criterio | Tarea |
|---|---|---|
| 1 | Sin sesión, enlace y no formulario; vuelve a la ficha | 7 |
| 2 | `volver` externo no redirige fuera | 6, 7 |
| 3 | Comprador envía el lead; al recargar no puede enviar otro | 4, 7 |
| 4 | El vendedor lo ve sin correo ni teléfono, contra el HTML servido | 3, 8, 9 |
| 5 | Tras aceptar aparecen; tras descartar, no | 5, 8 |
| 6 | Un lead ya respondido no cambia, y no se reporta como éxito | 5, 8 |
| 7 | Un vendedor no se deja un lead en su propia propiedad | 4 |
| 8 | No se capturan leads sobre propiedades no publicadas | 4 |
| 9 | Vendedor ajeno no ve nada, con falsificación | 5 |
| 10 | El comprador no puede falsificar el `vendedor_id` | 4 |
| 11 | El cuarto registro desde una IP en una hora se rechaza | 2 |
| 12 | Sin `IP_CABECERA_CONFIABLE`, el registro se rechaza | 2, 9 |
| 13 | Los tres eventos quedan en `registro_auditoria` | 4, 5 |
| 14 | Suites en verde, `tsc` limpio, `lint` sin avisos, guard pasando | 9 |
